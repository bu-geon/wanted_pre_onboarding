import Card from '../UI/Card';

const Dropdown = () => {
  return (
    <Card title='Dropdown'>
      <input type='search' placeholder='Search Symbol' />
    </Card>
  );
};

export default Dropdown;
